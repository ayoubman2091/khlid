# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env files — see DEPLOYMENT.md. .env.local files carry per-developer/secret values and
# stay untracked. .env.example (the documented template) and .env.production (the confirmed,
# non-secret production domain — see DEPLOYMENT.md #1) are intentionally tracked.
.env
.env.local
.env.*.local
!.env.example
!.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
