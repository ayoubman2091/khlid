# Confirmed production domain (client confirmation, 2026-08-20) — see DEPLOYMENT.md #1.
# This is the real, currently-live RK Pyrénées Construction domain (punycode for the
# accented "rk-pyrénées-construction.com"). Committed (not a secret) so the domain is
# tracked in version control as the single source of truth — see .gitignore.
#
# Loaded automatically by `vite build` (mode=production) and by scripts/generate-seo-files.ts
# / scripts/prerender.ts (via vite's loadEnv, matching the same resolution).
VITE_SITE_URL=https://xn--rkpyrnesconstruction-f2bb.com

# Google Search Console HTML-tag verification value (see DEPLOYMENT.md #7) — renders into the
# `google-site-verification` meta tag in index.html and every prerendered route (see
# scripts/prerender.ts). Not a secret (it's publicly visible in the page source once deployed),
# so it's committed here like VITE_SITE_URL above, as the single source of truth.
VITE_GSC_VERIFICATION=WiJKx7qmvB3cUPtZQXyx90UKOitb1WZXKXuQTIB5REQ
